# ============================================
# Docker / Coolify Environment Variables
# Copy to .env.docker and fill in the values
# ============================================

# ── Supabase (Required) ─────────────────────
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ── Frontend Build Args ─────────────────────
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
# IMPORTANT: Leave empty or set to your domain.
# Nginx proxies /api/* to backend internally,
# so the frontend does NOT need an external API URL.
VITE_API_BASE_URL=

# ── Ports ────────────────────────────────────
FRONTEND_PORT=80
BACKEND_PORT=3001

# ── CORS ─────────────────────────────────────
CORS_ORIGINS=https://yourdomain.com

# ── Rate Limiting ────────────────────────────
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100

# ── Email (Optional — fill if using) ────────
# RESEND_API_KEY=re_xxxxx
# SENDGRID_API_KEY=SG.xxxxx
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
