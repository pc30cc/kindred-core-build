; WEBYAR Telephony — built-in HTTP server (carries ARI + its WebSocket).
; Internal only: port 8088 is exposed on the container network, never published.

[general]
enabled = yes
bindaddr = 0.0.0.0
bindport = 8088
prefix =
enablestatic = no
