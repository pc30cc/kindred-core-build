; WEBYAR Telephony — logging to stdout so Docker/Coolify collect it.
; SIP passwords never appear: PJSIP debug is off and must stay off in production.

[general]
dateformat = %F %T

[logfiles]
console => notice,warning,error
