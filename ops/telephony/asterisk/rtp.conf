; WEBYAR Telephony — RTP media ports (template).
; Must match TELEPHONY_RTP_PORT_MIN/MAX in compose and the firewall.

[general]
rtpstart = ${TELEPHONY_RTP_PORT_MIN}
rtpend = ${TELEPHONY_RTP_PORT_MAX}
icesupport = no
strictrtp = yes
