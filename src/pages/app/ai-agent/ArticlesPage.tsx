// AI Agent — Articles tab (Phase 1 of KB unification).
// Renders the existing KnowledgeBasePage so all article authoring
// happens inside the AI Agent hub. No data or behavior is changed.
import KnowledgeBasePage from '@/pages/app/KnowledgeBasePage';

export default function AiAgentArticlesPage() {
  return <KnowledgeBasePage />;
}