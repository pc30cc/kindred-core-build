/**
 * Account → Availability. Personal operator working hours & presence.
 * Widget-level (workspace) business hours live under Widget settings.
 */
import { PersonalAvailabilityPanel } from '@/components/app/widget/PersonalAvailabilityPanel';

export default function SettingsAvailabilityPage() {
  return <PersonalAvailabilityPanel />;
}
